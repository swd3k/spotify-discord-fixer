# v1.2.0

- Полноценное десктоп-приложение (Electron): правка системного файла hosts напрямую, с запросом прав администратора.
- Получение списка прокси-узлов через резолв `geohide.ru` + проверка доступности узлов по TCP.
- Автоматическая резервная копия `hosts` перед каждым изменением; кнопка отката.
- Интерфейс полностью на русском языке.
- Исправлено переключение светлой/тёмной темы.
